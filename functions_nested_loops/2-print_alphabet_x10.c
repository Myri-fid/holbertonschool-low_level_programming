#include "main.h"
/**
*print_alphabet - check the code
*/
void print_alphabet(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z' ; letter++)

	if
		(letter == 10)
	{
		_putchar(letter);
	}
	_putchar('\n');
}
