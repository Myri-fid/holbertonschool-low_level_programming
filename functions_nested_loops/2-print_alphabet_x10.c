#include "main.h"
/**
*print_alphabet_x10 - check the code
*/
void print_alphabet_x10(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z' ; letter++)

	if
		(letter == 10)
	{
		_putchar(letter);
	}
	_putchar('\n');
}
