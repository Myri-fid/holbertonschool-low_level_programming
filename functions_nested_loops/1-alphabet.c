#include "main.h"

/**
 *main - _putchar charactere
 *
 * Return: 0
 */
int main(void)
{
	int alphabet;

	for
		(alphabet = 'a' && alphabet <= 'z', alphabet++)
	{
			_putchar(alphabet);
	}
	_putchar('\n');
	return (0);
}
