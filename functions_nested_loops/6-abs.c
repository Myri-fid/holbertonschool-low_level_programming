#include "main.h"
#include <stdlib.h>
/**
*_abs - computes the absolute value of an integer.
* @n: The integer
* Return: Always 0.
*/
int _abs(int n)
{
	int n = -number, res;

	_putchar("%d\n", n);
	res = abs(n);
	_putchar("%d", res);
}
