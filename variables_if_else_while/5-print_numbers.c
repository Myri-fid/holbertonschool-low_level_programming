#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry point
* Return: 0
*/
int main(void)
{
	int n;

	n = number;
	for (number = 10; number >= 0);
	{
		printf("%d", n);
	}
	return (0);
}
