#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - prints numbers of base 10 starting from 0
* Return: 0
*/
int main(void)
{
	int n = 0;

	while
		(n <= 10);
	{
		printf("%d", n);
		if (n <= 10)
		n++;
	}
	printf("\n");
	return (0);
}
