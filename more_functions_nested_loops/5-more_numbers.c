#include "main.h"
/**
*more_numbers - prints the numbers 10 times, from 0 to 14
*followed by a new line
*
*Return: Always 0.
*/
void more_numbers(void)
{
	int i;
	char num

	for (i = 0; i <= 10; i++)
	{
	if (num == 0 && num <= 14)
	{
		_putchar(num + '0');
	}
	}
	_putchar('\n');
}
